# 📦 Next.js Font Optimization - সম্পূর্ণ নির্দেশিকা

এই প্রজেক্টে Next.js ফন্ট অপ্টিমাইজেশনের সব ধরনের নিয়ম ও বেস্ট প্র্যাকটিস অন্তর্ভুক্ত রয়েছে।

## 🚀 Font Optimization পদ্ধতিসমূহ

### 1. **Next.js Built-in `next/font`**
```javascript
// app/layout.js বা pages/_app.js
import { Inter, Roboto_Mono } from 'next/font/google'

const inter = Inter({
  subsets: ['latin', 'bengali'],
  display: 'swap',
})

const robotoMono = Roboto_Mono({
  subsets: ['latin'],
  display: 'swap',
})

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={inter.className}>
      <body>
        <div className={robotoMono.className}>{children}</div>
      </body>
    </html>
  )
}
```
![Public folder](/public/Img/local-fonts.png)

### 2. **কাস্টম ফন্ট (Local Fonts)**
```javascript
import localFont from "next/font/local";
import "./globals.css";

// একক ফন্ট ফাইলের জন্য
const myFont = localFont({
  src: '../../public/local fonts/Nikosh/Nikosh.ttf'
})

// একাধিক ফন্ট ফাইল (Multiple weights/styles)
const myFontFamily = localFont({
  src: [
    {
      path: '../../public/local fonts/SolaimanLipi/SolaimanLipi-Thin.ttf',
      weight: '100',
      style: 'thin',
    },
    {
      path: '../../public/local fonts/SolaimanLipi/SolaimanLipi-Normal.ttf',
      weight: '400',
      style: 'normal',
    },
    {
      path: '../../public/local fonts/SolaimanLipi/SolaimanLipi-Bold.ttf',
      weight: '700',
      style: 'bold',
    },
    {
      path: '../../public/local fonts/SolaimanLipi/SolaimanLipi-Black.ttf',
      weight: '900',
      style: 'black',
    }
  ],
  variable: '--font-solaimanlipi', // CSS variable হিসেবে ব্যবহারের জন্য
  display: 'swap',
})

export const metadata = {
  title: "Next.js Font Optimization",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={myFontFamily.variable}>
      <body className={myFontFamily.className}>
        {children}
      </body>
    </html>
  );
}
```

### 3. **একাধিক ফন্ট একসাথে ব্যবহার (Multiple Fonts)**
```javascript
import localFont from "next/font/local";
import "./globals.css";

// সোলাইমানলিপি ফন্ট সেটআপ (Multiple weights সহ)
const solaimanFont = localFont({
  src: [
    {
      path:'../../public/local fonts/SolaimanLipi/SolaimanLipi-Thin.ttf',
      weight: '100',
      style: 'thin',
    },
    {
      path:'../../public/local fonts/SolaimanLipi/SolaimanLipi-Normal.ttf',
      weight: '400',
      style: 'normal',
    },
    {
      path:'../../public/local fonts/SolaimanLipi/SolaimanLipi-Bold.ttf',
      weight: '700',
      style: 'bold',
    },
  ],
  variable: '--font-solaiman',
  display: 'swap',
});

// নিকশ ফন্ট সেটআপ
const nikoshFont = localFont({
  src: [
    {
      path: '../../public/local fonts/Nikosh/Nikosh.ttf',
      weight: '400',
      style: 'normal',
    },
  ],
  variable: '--font-nikosh',
  display: 'swap',
})

// ফন্ট এক্সপোর্ট করুন
export { nikoshFont, solaimanFont };

export const metadata = {
  title: "Next.js Font Optimization",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    // একাধিক ফন্ট variable একসাথে ব্যবহার
    <html lang="en" className={`${solaimanFont.variable} ${nikoshFont.variable}`}>
      {/* মূল ফন্ট body তে অ্যাপ্লাই করুন */}
      <body className={solaimanFont.className}>
        {children}
      </body>
    </html>
  );
}

 // চাইলে bodyর মধ্যে আমরা দিতে পারি
export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${solaimanFont.variable} ${nikoshFont.variable}`}>
      <body className={`${nikoshFont.className} text-sm`}>
        {children}
      </body>
    </html>
  );
}
```

### **পেজে ফন্ট ব্যবহার:**
```javascript
// app/page.js
import { nikoshFont } from "./layout";

export default function Home() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center py-2">
      <h1 className='text-lg'>সোলাইমানলিপি বাংলা ফন্ট</h1>
      <h1 className={`${nikoshFont.className} text-sm`}>নিকশ বাংলা ফন্ট</h1>
    </div>
  );
}
```

### **CSS Variables ব্যবহার করে ফন্ট পরিবর্তন:**
```css
/* globals.css */
:root {
  --font-primary: 'SolaimanLipi', sans-serif;
  --font-secondary: 'Nikosh', sans-serif;
}

/* ব্যবহার */
.primary-text {
  font-family: var(--font-solaiman);
}

.secondary-text {
  font-family: var(--font-nikosh);
}
```

### 4. **Google Fonts অপ্টিমাইজেশন**
```javascript
import { GoogleFont } from 'next/font/google'

// সাবসেট নির্ধারণ
const font = GoogleFont({
  family: 'Noto Sans Bengali',
  subsets: ['bengali', 'latin'],
  weights: [400, 500, 600, 700],
  preload: true,
  fallback: ['system-ui', 'arial'],
})
```

## ⚡ অপ্টিমাইজেশন টিপস

### **Performance Best Practices:**
1. **Preload কোর ফন্টস:** শুধুমাত্র প্রয়োজনীয় ওজন প্রিলোড করুন
2. **সাবসেট ব্যবহার:** শুধুমাত্র প্রয়োজনীয় ভাষা সাবসেট যোগ করুন
3. **`display: 'swap'`:** FOIT/FOUT সমস্যা সমাধান
4. **Variable Fonts:** একাধিক ওজন ম্যানেজ করা সহজ
5. **Font Loading Strategy:** কৌশলগতভাবে লোডিং ম্যানেজ করুন
6. **একাধিক ফন্ট ব্যবস্থাপনা:** Primary ফন্ট body তে, Secondary ফন্ট variable হিসেবে

### **একাধিক ফন্ট ব্যবস্থাপনা নীতিমালা:**

```javascript
// ✅ সঠিক পদ্ধতি: Primary font body তে, secondary fonts variables হিসেবে
export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${primaryFont.variable} ${secondaryFont.variable}`}>
      <body className={primaryFont.className}>
        {children}
      </body>
    </html>
  );
}

// ❌ ভুল পদ্ধতি: সব ফন্ট body তে
<body className={`${font1.className} ${font2.className}`}>
```

### **ফাইল ফরম্যাট প্রায়োরিটি:**
1. `.woff2` (সর্বোত্তম কম্প্রেশন)
2. `.woff`
3. `.ttf`
4. `.otf`

### **একাধিক ফন্ট ফাইল ব্যবস্থাপনা:**
```javascript
// বিভিন্ন weight এবং style একসাথে ব্যবহার
const customFont = localFont({
  src: [
    // Regular variants
    {
      path: './fonts/MyFont-Regular.woff2',
      weight: '400',
      style: 'normal',
    },
    {
      path: './fonts/MyFont-Italic.woff2',
      weight: '400',
      style: 'italic',
    },
    
    // Bold variants
    {
      path: './fonts/MyFont-Bold.woff2',
      weight: '700',
      style: 'normal',
    },
    {
      path: './fonts/MyFont-BoldItalic.woff2',
      weight: '700',
      style: 'italic',
    },
    
    // Light variants
    {
      path: './fonts/MyFont-Light.woff2',
      weight: '300',
      style: 'normal',
    },
  ],
  
  // CSS variable হিসেবে ব্যবহার
  variable: '--font-custom',
  
  // Performance optimization
  display: 'swap',
  preload: true,
  
  // Fallback fonts
  fallback: ['system-ui', 'arial'],
  
  // Adjust font metrics
  adjustFontFallback: true,
})

// ব্যবহার করার সময়
export default function Layout({ children }) {
  return (
    <html lang="en" className={customFont.variable}>
      <body className={customFont.className}>
        {/* বিভিন্ন weight ব্যবহার */}
        <h1 className="font-bold">Bold Text</h1>
        <p className="font-normal">Regular Text</p>
        <p className="font-light italic">Light Italic Text</p>
      </body>
    </html>
  )
}
```

### **CSS Variables মাধ্যমে Weight কন্ট্রোল:**
```css
/* globals.css */
:root {
  --font-weight-thin: 100;
  --font-weight-light: 300;
  --font-weight-regular: 400;
  --font-weight-medium: 500;
  --font-weight-bold: 700;
  --font-weight-black: 900;
}

/* ব্যবহার */
.thin-text {
  font-weight: var(--font-weight-thin);
}

.bold-text {
  font-weight: var(--font-weight-bold);
}
```

## 📊 মেট্রিকস মনিটরিং

```javascript
// next.config.js
module.exports = {
  experimental: {
    fontLoaders: [
      { loader: '@next/font/google', options: { subsets: ['latin'] } }
    ],
  },
}
```

## 🛠️ ট্রাবলশুটিং

### সাধারণ সমস্যা ও সমাধান:
1. **ফন্ট ফ্লিকারিং:** `display: 'swap'` ব্যবহার করুন
2. **লেআউট শিফট:** `font-size-adjust` প্রোপার্টি ব্যবহার করুন
3. **ধীর লোডিং:** CDN ব্যবহার করুন এবং ফন্ট সাবসেটিং করুন
4. **একাধিক ফন্ট ফাইল লোড:** শুধুমাত্র প্রয়োজনীয় weights প্রিলোড করুন
5. **একাধিক ফন্ট কনফ্লিক্ট:** Primary font body তে, secondary fonts variables হিসেবে রাখুন

### **একাধিক ফন্ট ব্যবস্থাপনা টিপস:**
```javascript
const optimizedFont = localFont({
  src: [
    // শুধুমাত্র প্রয়োজনীয় weights লোড করুন
    {
      path: './fonts/Primary-Regular.woff2',
      weight: '400', // শুধুমাত্র regular weight
    },
    {
      path: './fonts/Primary-Bold.woff2',
      weight: '700', // শুধুমাত্র bold weight
    },
  ],
  // বাকি weights-এর জন্য fallback ব্যবহার করুন
  fallback: ['system-ui', 'Segoe UI', 'Roboto', 'Arial'],
  
  // Performance optimization
  preload: true, // শুধুমাত্র preload critical fonts
  display: 'optional', // optional swap for better performance
})
```

## 🌐 বাংলা ফন্ট অপ্টিমাইজেশন

```javascript
// বাংলা ফন্টের জন্য বিশেষ সেটিংস
const bengaliFont = GoogleFont({
  family: 'Hind Siliguri',
  subsets: ['bengali'],
  adjustFontFallback: true,
  preload: true,
})

// বাংলা লোডাল ফন্টের জন্য
const bengaliLocalFont = localFont({
  src: [
    {
      path: './fonts/Bangla-Regular.woff2',
      weight: '400',
      style: 'normal',
    },
    {
      path: './fonts/Bangla-Bold.woff2',
      weight: '700',
      style: 'normal',
    },
  ],
  preload: true,
  display: 'swap',
  fallback: ['Noto Sans Bengali', 'Arial', 'sans-serif'],
})

// একাধিক বাংলা ফন্টের উদাহরণ
const solaimanFont = localFont({
  src: './fonts/SolaimanLipi.ttf',
  variable: '--font-solaiman',
})

const nikoshFont = localFont({
  src: './fonts/Nikosh.ttf',
  variable: '--font-nikosh',
})

// Layout-এ ব্যবহার
export default function Layout({ children }) {
  return (
    <html lang="bn" className={`${solaimanFont.variable} ${nikoshFont.variable}`}>
      <body className={solaimanFont.className}>
        {/* প্রাইমারি ফন্ট হিসেবে সোলাইমানলিপি */}
        {children}
      </body>
    </html>
  )
}
```

## 📈 Best Practices Summary

1. **Preload Critical Fonts Only:** শুধুমাত্র প্রথম পেজের জন্য প্রয়োজনীয় ফন্ট প্রিলোড করুন
2. **Use WOFF2 Format:** সর্বোত্তম কম্প্রেশন এবং পারফরম্যান্স
3. **Limit Font Weights:** সাধারণত 2-3টি weight (regular, bold) যথেষ্ট
4. **Implement Font Display Swap:** FOIT/FOUT সমস্যা সমাধান
5. **Use CSS Font Display API:** বেটার কন্ট্রোলের জন্য
6. **Monitor Performance:** Web Vitals ব্যবহার করে মনিটর করুন
7. **Implement Fallback Fonts:** ফন্ট লোড না হলে alternative
8. **Multiple Font Strategy:** Primary ফন্ট body তে, Secondary ফন্ট variable হিসেবে
9. **Font Export:** ফন্টগুলো export করুন যাতে অন্য পেজে ব্যবহার করা যায়
10. **Variable Management:** CSS variables দিয়ে ফন্ট পরিবর্তন করুন

---

## 🎯 Advanced: Dynamic Font Loading

```javascript
// Font utilities
export const fonts = {
  solaiman: {
    className: solaimanFont.className,
    variable: solaimanFont.variable,
  },
  nikosh: {
    className: nikoshFont.className,
    variable: nikoshFont.variable,
  },
};

// Conditional font loading
export const getFont = (fontName) => {
  switch(fontName) {
    case 'solaiman':
      return solaimanFont.className;
    case 'nikosh':
      return nikoshFont.className;
    default:
      return solaimanFont.className;
  }
};
```

## 🔧 Font Loading Performance Tips

1. **Lazy Load Fonts:** Non-critical fonts lazy load করুন
2. **Font Loading API:** `font-display: optional` ব্যবহার করুন
3. **Font Face Observer:** Font loading detect করার জন্য
4. **Critical CSS:** Above-the-fold content-এর ফন্ট inline করুন

---

## 🧠 `<html>` vs `<body>` এ `className` ব্যবহারের পার্থক্য

Next.js (বিশেষ করে `app/layout.js`)–এ ফন্ট বা থিম ব্যবস্থাপনার সময় `<html>` এবং `<body>`–তে class দেওয়ার প্রভাব আলাদা। সঠিক জায়গায় সঠিক class ব্যবহার করা খুব গুরুত্বপূর্ণ।

---

### 1️⃣ `<html>` এ class দিলে কী হয়

```jsx
<html className="font-solaiman font-nikosh">
```

### ✅ প্রভাব

* পুরো **ডকুমেন্ট লেভেলে** কাজ করে
* DOM hierarchy অনুযায়ী
  `html → body → সব elements`
  এই class–এর ভিতরে পড়ে
* সরাসরি টেক্সটে ফন্ট apply করে না
* সাধারণত **CSS variables ও global state বহন** করে

### ✅ কখন ব্যবহার করবেন

* একাধিক font variable define করতে হলে
* Global font system তৈরি করতে
* Dark / Light mode toggle করতে
* Global CSS variables দরকার হলে
* Theme বা design token রাখার জন্য

### 📌 উদাহরণ

```css
/* globals.css */
.font-solaiman {
  --font-solaiman: 'SolaimanLipi';
}

.font-nikosh {
  --font-nikosh: 'Nikosh';
}

body {
  font-family: var(--font-solaiman);
}
```

🔎 **ব্যাখ্যা:**
এখানে `<html>` শুধু variable বহন করছে।
ফন্ট সরাসরি text–এ apply হচ্ছে না, বরং `body` সেই variable ব্যবহার করছে।

👉 **Best practice:**
`<html>` = variable / configuration holder

---

### 2️⃣ `<body>` তে class দিলে কী হয়

```jsx
<body className={solaimanFont.className}>
```

### ✅ প্রভাব

* মূল কনটেন্টের উপর **সরাসরি apply হয়**
* সব text, paragraph, heading, button, link—সব জায়গায় ফন্ট কাজ করে
* Default font হিসেবে সেট হয়

### ✅ কখন ব্যবহার করবেন

* Default / primary font set করতে চাইলে
* Layout, spacing, background, text color সেট করতে
* App–wide typography control করতে

### 📌 উদাহরণ

```css
body {
  font-family: 'SolaimanLipi';
}
```

👉 এই ক্ষেত্রে আলাদা করে variable দরকার নেই
👉 ফন্ট সরাসরি DOM–এ apply হচ্ছে

---

### 🟢 Recommended Pattern (Best Practice)

```jsx
<html lang="bn" className={`${solaimanFont.variable} ${nikoshFont.variable}`}>
  <body className={solaimanFont.className}>
    {children}
  </body>
</html>
```

### কেন এই প্যাটার্ন সেরা?

| Element   | দায়িত্ব                              |
| --------- | ------------------------------------ |
| `<html>`  | Font variables, theme, global config |
| `<body>`  | Primary / default font apply         |
| Component | Secondary font selectively apply     |

---

### ❌ ভুল পদ্ধতি (Avoid This)

```jsx
<body className={`${solaimanFont.className} ${nikoshFont.className}`}>
```

🚫 এতে:

* একাধিক font একসাথে apply হয়
* CSS conflict হয়
* Performance ও maintainability খারাপ হয়

---

### 🧠 মনে রাখবেন

* `<html>` → **configuration layer**
* `<body>` → **presentation layer**
* Font system clean রাখতে হলে এই separation খুব জরুরি

---